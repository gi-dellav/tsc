"""Custom key bindings."""

from prompt_toolkit.key_binding import KeyBindings


def create_keybindings() -> KeyBindings | None:
    """Return custom key bindings, or None for defaults."""
    # ponytail: stub — add custom bindings (e.g. C-c to clear line)
    return None
