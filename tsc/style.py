"""Styling for the REPL."""

from prompt_toolkit.styles import Style


def create_style() -> Style | None:
    """Return a Style instance, or None for the default style."""
    # ponytail: stub — return Style.from_dict({...})
    return None
